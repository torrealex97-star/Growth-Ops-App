# Handoff — 12 septiembre 2026

## Estado seguro alcanzado

- Rama local: `codex/architecture-closeout`.
- Base: `origin/claude/app-continuation-lpbupf` (`3904038`).
- Commit ya creado: `a745af6 fix: unblock builds and enforce 301 redirects`.
- El build de producción, typecheck, tests y métricas pasaron antes de este cierre; lint pasó con avisos existentes.
- Se corrigieron cuatro rutas de documentos que creaban el cliente Supabase en import-time y rompían el build sin variables.
- Las redirecciones antiguas de Marketing ahora son HTTP 301 reales y tienen prueba de regresión.

## Supabase aplicado y verificado

Proyecto: `Growth Ops` (`rgcbveflosqgxrcqlqzv`). Solo existe la rama `main`; no hay staging de Supabase.

Se aplicaron correctamente estas migraciones aditivas:

1. `composite_perf_indexes`
2. `tenant_branding`
3. `carruseles_tenant_isolation`
4. `amount_range_constraints`

Verificación posterior:

- 6/6 índices esperados presentes.
- 4/4 constraints de rango presentes.
- 6 políticas RLS tenant-scoped en las tres tablas de Carruseles.
- 12 grants esperados para `authenticated` (SELECT/INSERT/UPDATE/DELETE); no se concedió acceso a `anon`.
- Branding Evergreen = Scalix Systems/acento brand.
- Branding Women Digital Closer = WDC/acento pink.
- Antes de aplicar los constraints se confirmó que no había filas históricas con importes inválidos.

La migración local `20260912120000_carruseles_tenant_isolation.sql` fue reparada porque producción no tenía las tablas `carrusel_projects`, `carrusel_templates` ni `carrusel_brand`. Ahora las crea con checks, FK, grants y RLS. La migración de constraints ahora es idempotente.

## Hallazgos y bloqueos reales

- Producción Vercel todavía sirve el despliegue anterior: muestra branding antiguo y la sección DIRECCIÓN. Los cambios avanzados aún no se han desplegado.
- GitHub no permite proteger `main` en este repositorio privado con el plan Free (API devuelve 403). Requiere hacerlo público o subir a Pro/Team.
- El bucket `contratos` no existe actualmente. No crear/cambiar almacenamiento de contratos hasta definir el modelo privado (`signed_pdf_path` + URLs firmadas) y revisar los cuatro consumidores.
- No se añadió todavía la migración de políticas de `users`: requiere revisar cuidadosamente las políticas y funciones SECURITY DEFINER existentes para no introducir recursión o escalada cross-tenant.
- No se ejecutó aún el restore drill: debe hacerse en staging/nuevo proyecto, nunca sobre producción.
- Sentry/observabilidad externa requiere proyecto y DSN, o una decisión explícita de usar otra solución.

## Siguiente secuencia recomendada

1. Ejecutar el Quality Gate local completo sobre los dos SQL modificados y crear el siguiente commit.
2. Hacer una prueba RLS cross-tenant real y transaccional para Carruseles (usuario tenant A, usuario tenant B, anonymous; SELECT/INSERT/UPDATE/DELETE; rollback de fixtures).
3. Inspeccionar `users`, `tenant_members`, `auth_tenant_ids()` e `is_super_admin()` y diseñar la política de usuarios sin `USING (true)` ni funciones SECURITY DEFINER públicas innecesarias.
4. Push de `codex/architecture-closeout`, abrir PR nuevo y desplegar Preview de Vercel. No reutilizar PR #4: parte de un `main` antiguo.
5. QA visual en Preview: WDC rosa/nombre en login, recovery, loading, sidebar; ausencia de DIRECCIÓN; Marketing y rutas 301; desktop/tablet/móvil.
6. Solo tras PASS, decidir merge/despliegue a producción.
7. Después iniciar `PROMPT_UXUI_AUDIT.md`; no mezclar ese rediseño grande con el cierre de arquitectura/seguridad.

## Archivos guía obligatorios

- `AGENTS.md`
- `CLAUDE.md`
- `docs/DEVELOPMENT_RULES_FULL.md`
- `docs/METRICS.md`
- `docs/PRODUCTION_READINESS.md`
- `/Users/alextorre/Downloads/PROMPT_ARQUITECTURA_PENDIENTE.md`
- `/Users/alextorre/Downloads/PROMPT_UXUI_AUDIT.md`

No declarar PASS para ramas protegidas, E2E, RLS cross-tenant, restore drill, Sentry ni QA visual: siguen pendientes o bloqueados.
