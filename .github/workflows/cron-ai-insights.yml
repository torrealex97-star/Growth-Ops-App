# Cron retirado de vercel.json (Vercel Hobby: máx. 2 crons). GitHub Actions lo ejecuta con el
# mismo horario UTC y la misma autenticación (Bearer CRON_SECRET) que usaba Vercel Cron.
name: "cron: ai-insights"
on:
  schedule:
    - cron: '0 5 * * *' # diario 05:00 UTC — insights proactivos deterministas (7d vs 7d previos)
  workflow_dispatch:
permissions: {}
concurrency:
  group: cron-ai-insights
  cancel-in-progress: false
jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: GET /api/_/evergreen/cron/ai-insights
        env:
          APP_URL: ${{ vars.CRON_APP_URL || 'https://growth-ops-weld.vercel.app' }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          set -euo pipefail
          code=$(curl -sS -o response.json -w '%{http_code}' --max-time 120 \
            -H "Authorization: Bearer $CRON_SECRET" \
            "$APP_URL/api/_/evergreen/cron/ai-insights")
          echo "HTTP $code"
          cat response.json; echo
          case "$code" in 2*) exit 0 ;; *) exit 1 ;; esac
